{
  "name": "sass-compile",
  "version": "1.0.0",
  "description": "A simple sass compile config.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" &amp;&amp; exit 1",
    "sass": "node-sass scss/style.scss style.css --watch --update --output-style=expanded --indent-type=tab --indent-width=1 --source-map=true --source-map-contents"
  },
  "author": "Adam",
  "license": "ISC",
  "dependencies": {
    "node-sass": "^9.0.0"
  }
}
